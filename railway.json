{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "npx prisma generate && node server.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120
  }
}
